---
title: "Ⅷ. 컴퓨팅 사고Ⅷ"
author: "ne_choi"
date: '2021 01 13 '
output:
  html_document:
   toc: true
   toc_float:
     collapsed: false
     smooth_scroll: true
   theme: united
   highlight: textmate
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

* POSTECH에서 제공하는 [MOOC](https://pabi.smartlearn.io/) 중, 컴퓨팅 사고 Ⅱ 과정입니다.

# Ⅷ. 순컴퓨팅 사고Ⅷ
## 1. 순서열 연산자
### 순서열 연산자
- '더미'의 의미에 맞게 빠뜨림 없이
  - '더미'의 의미에 맞게 작동
  - '더미'로 정의한 순서열 원소를 빠뜨림 없이 처리

### 순서열 연산자 설계 검증
1. 순서열을 만든 직후, 원소나 원소 더미를 뽑아내어 검사  
2. 내용을 모르는 순서열을 받아서 복제할 수 있는지 검사

### 순서열 연산자 설계
- 다음 유도하기에 맞추어 순서열 만들기
  - 빈 순서열: []
  - 작은 순서열 맨 앞에 원소 하나를 덧붙여 얻은 순서열: [원소] + 작은 순서열
- 필요한 연산자
  1. 두 가지를 구분하는 연산자
  2. 빈 순서열이 아닐 때, 맨 앞 원소와 작은 순서열을 계산하는 연산자
     - 순서열을 빈 순서열과 비교: 순서열 == []
     - 순서열 맨 앞 원소 계산: 순서열[0]
     - 순서열 맨 앞 원소 이후의 작은 순서열 계산: 순서열[1:]

### 순서열 복제하기
- 사용하는 연산자
  - 순서열 == []
  - 순서열[0]
  - 순서열[1:]
- 상수 op에 기록된 순서열 복제하기
  - [] if op == [] else [op[0]] + op[1:]

### 하나로 통합한 순서열 연산자
- 상수 op에 순서열이 기록되어 있을 때
  - (빈 순서열일 때 연산식 if op == [] else
  lambda hd, tl: 빈 순서열이 아닐 때 연산식)(op[0], op[1:]))
    - hd = head
    - tl = tail
    